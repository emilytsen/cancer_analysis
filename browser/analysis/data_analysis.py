# Caso você precise de funções específicas para análise de dados, pode organizá-las aqui.

def analyze_data(data):
    # Realize análises dos dados, como calcular estatísticas, filtrar dados, etc.
    # Exemplo:
    mean_deaths = data['Total Deaths'].mean()
    max_deaths = data['Total Deaths'].max()
    return mean_deaths, max_deaths

